package com.codiply.barrio.helpers

object ConfigKey {
  val akkaSystem = "barrio.akka-system"
  val defaultRequestTimeoutMilliseconds = "barrio.default-request-timeout-milliseconds"
  val hostname = "barrio.hostname"
  val maxRequestTimeoutMilliseconds = "barrio.max-request-timeout-milliseconds"
  val webApiPort = "barrio.web-api-port"
}
